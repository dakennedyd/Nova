#pragma once

namespace Nova {
	namespace tests {		
		/*void testVec3(const int iterations);
		void testMat3(const int iterations);
		void testMat4(const int iterations);
		void testQuat(const int iterations);
		void testMatLibrary();
		void error(const std::string &failedFunctionName, const float calculatedValue, const float correctValue);
		*/
	}
}
